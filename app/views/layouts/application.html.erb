<!DOCTYPE html>
<html>
  <head>
    <title>GoodThings</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <meta name="viewport" content="width=device-width, initial-scale=1">
  </head>

  <body>
    <div class="container-fluid">

      <header class="row" style="margin-top: 20px;">
        <div class = "col-xs-offset-1 col-xs-9">
          <ul class="nav nav-pills">
            <li class="active"><a href="#">Главная</a></li>
            <li><a href="#">Профиль</a></li>
            <li><a href="#">Сообщение</a></li>
            <li><a href="/organizations">Organizations</a></li>
            <li><%= link_to "Sign up", new_user_registration_path %></li>
          </ul>
        </div>
        <div class="col-xs-2">
          <%if user_signed_in? %>
              <div style="display:inline-block" class="round-image-50"><%= image_tag(current_user.avatar.url(:thumb)) %></div>
              <%= link_to(t('.edit_user'), edit_user_registration_path) %>
              <%= link_to(t('.sign_out'), destroy_user_session_path, method: :delete) %>
          <%else%>
              <%= link_to(t('.sign_in'), new_user_session_path) %>
          <%end%>
        </div>
      </header>

      <div class="row">
        <% flash.each do |name, msg|%>
          <%=msg%>
        <%end%>
        <div class="tbody col-xs-10 col-xs-offset-1" style = "margin-top:50px; min-height: 680px;">
          <%= yield %>
        </div>
      </div>

      <footer class="col-xs-12">
        <div class="center-block">
          <p> Good Things, <%= Time.now.to_datetime.year %> </p>
        </div>
      </footer>
    </div>
  </body>
</html>
